This project was created using Vite TS template. You can also scaffold the template using the command:

```
npm create vite@latest --template react-ts
```

1. To install all dependencies:

```
npm install
```

2. To run the project locally:

```
npm run dev
```
